<Page
    x:Class="NewsParserApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewsParserApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel Margin="20">

            <Button x:Name="ParseButton" Content="Парсить новости" Click="ParseButton_Click"/>

            <TextBlock x:Name="OutputTextBlock" TextWrapping="Wrap" Margin="0,20,0,0"/>

            <ListView x:Name="NewsListView" 
                          SelectionChanged="NewsListView_SelectionChanged" 
                          Margin="0,20,0,0"
                          ScrollViewer.IsVerticalRailEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" 
                                    FontSize="16" 
                                    TextWrapping="WrapWholeWords"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>    
         </StackPanel>
    </Grid>
</Page>
