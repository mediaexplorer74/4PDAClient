<Page x:Class="FourPDA.Views.MainPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:FourPDA.Views"
      RequestedTheme="Dark"
      xmlns:mpc="using:FourPDA.Controls"
      xmlns:ctrl="clr-namespace:FourPDA.Controls"
      xmlns:micro="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
      xmlns:cnv="clr-namespace:FourPDA.Converters"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      mc:Ignorable="d"
      >
    <!--      
       xmlns:mainPivot1="clr-namespace:FourPDA.Views.MainPivot"
    -->

    <Page.Resources>
        <SolidColorBrush 
            x:Key="NavigationViewExpandedPaneBackground" />
    </Page.Resources>

    
    <!-- ************* Some "decorative" user control **************************  -->
    <mpc:MainControl />
    <!--  *************************************************************************** -->

    <!-- Bottom app bar -->
    <Page.BottomAppBar>
      <CommandBar>
        <CommandBar.PrimaryCommands>
          <AppBarButton
             x:Name="Back"
             x:Uid="Back"
             Click="Back_Click"
             Icon="Back"
             Label="Back"/>

          <AppBarButton
             x:Name="Forward"
             x:Uid="Forward"
             Click="Forward_Click"
             Icon="Forward"
             Label="Forward"/>

          <AppBarButton
             x:Name="Forum"
             x:Uid="Forum"
             Click="Forum_Click"
             Icon="NewWindow"
             Label="Forum"/>

           <AppBarButton
              x:Name="News"
              x:Uid="News"
              Click="News_Click"
              Icon="Globe"
              Label="News"/>
      
        </CommandBar.PrimaryCommands>


         <CommandBar.SecondaryCommands>

            <AppBarButton
            x:Name="Refresh"
            x:Uid="Refresh"
            Click="Refresh_Click"
            Icon="Refresh"
            Label="Refresh"/>

            <AppBarButton
            x:Name="Settings"
            x:Uid="Settings"
            Click="Settings_Click"
            Icon="Setting"
            Label="Settings"/>

            <AppBarToggleButton
             x:Name="UiThemeToggle"
             x:Uid="UiThemeToggle"
             Click="UiThemeToggle_Click"
             Icon="Clock"
             Label="Dark theme"/>
             
            <AppBarToggleButton
             x:Name="ForumModeToggle"
             x:Uid="ForumModeToggle"
             Click="ForumModeToggle_Click"
             Icon="Document"
             Label="Forum mode"/>

            <AppBarButton
             x:Name="AboutMe"
             x:Uid="AboutMe"
             Click="AboutApp_Click"
             Icon="Contact"
             Label="About"/> 
             
         </CommandBar.SecondaryCommands>
       </CommandBar>
    </Page.BottomAppBar>
</Page>
